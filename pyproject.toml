[tool.uv.workspace]
members = [
    "packages/*",
    "src/dev_agent",
    "src/stock_agent",
]
[tool.uv]
dev-dependencies = ["pytest>=8.0", "ruff>=0.4"]

[tool.uv.sources]
shared = { workspace = true }
