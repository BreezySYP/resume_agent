[tool.uv.workspace]
members = [
    "packages/*",
    "src/dev_agent",
    "src/stock_agent",
    "src/stock_etl",
    "src/stock_dashboard"
]
[tool.uv]
dev-dependencies = ["pytest>=8.0", "ruff>=0.4"]

[tool.uv.sources]
shared = { workspace = true }
